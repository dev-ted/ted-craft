# @ted-craft/web

TanStack Start + Fumadocs marketplace for ted-craft.

```bash
# from repo root
npm run generate
npm run dev
```

Vercel: set project root to `apps/web` (or use repo-root `vercel.json` build commands).
